export type { MatchMeta, WorldSettings, WorldState } from './state';
export { createInitialWorldState } from './state';

/** Pitch stub — retained from foundation scaffold. */
export interface PitchDimensions {
  width: number;
  height: number;
}
