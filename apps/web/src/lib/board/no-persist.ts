/**
 * LFE-BOARD-01 — M2 Explicit No Persist.
 * Board Thin has no IO module, no actions, no DB table.
 * This file documents the LOCK; do not add persist helpers here.
 */
export const BOARD_THIN_NO_PERSIST = true as const;
