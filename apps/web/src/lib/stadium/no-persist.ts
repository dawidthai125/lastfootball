/**
 * LFE-STADIUM-01 — M2 Explicit No Persist.
 * Stadium Thin has no IO module, no actions, no DB table, no ticket ledger.
 * This file documents the LOCK; do not add persist helpers here.
 */
export const STADIUM_THIN_NO_PERSIST = true as const;
